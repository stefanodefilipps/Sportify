

- if @m.tipo == 1
	- @players.each do |p|
		%ul
			%ul
				%li 
					= p.ruolo
				%li 
					= p.user.nick
				%form{:action => "https://localhost:3000/users/1/matches/"+@m.id.to_s+"/rate", :method => "post"}
					%p	
						%input{:type => "number", :name => "nick", :value=> p.user.nick}/
						%input{:type => "number", :name => "voto"}/
						%input{:type => "submit", :value => "rate player"}/

%form{:action => "https://localhost:3000/users/1/matches/"+@m.id.to_s+"/endgame", :method => "post"}
	%p
		%input{:type => "text", :name => "punt1"}/
		%input{:type => "text", :name => "punt2"}/
		%input{:type => "submit", :value => "fine partita!"}/

%form{:action => "https://localhost:3000/users/1/matches/"+@m.id.to_s+"/leavep", :method => "get"}
	%p
		%input{:type => "submit", :value => "lascia evento"}/
%form{:action => "https://localhost:3000/users/:id/matches/"+@m.id.to_s+"/redirect", :method => "get"}
	%p
		%input{:type => "submit", :value => "aggiungi a google calendar"}/
%form{:action => "https://localhost:3000/users/:id/matches/"+@m.id.to_s+"/destroy", :method => "get"}
	%p
		%input{:type => "submit", :value => "distruggi evento"}/
		
- if @m.tipo == 2
	- @single_players.each do |sp|
		%ul
			%ul
				%li 
					= p.ruolo
				%li 
					= p.user.nick
	- if @m.pt.team != nil
		SQUADRA
		= m.pt.team.nome
		- @team_players.each do |sp|
			%ul
				%ul
					%li 
						= p.ruolo
					%li 
						= p.user.nick
- if @m.tipo == 3
	- @players_team_1.each do |p|
		%ul
			%ul
				%li 
					= p.ruolo
				%li 
					= p.user.nick
	- if @m.tt.team.size == 2
		- @players_team_2.each do |p|
			%ul
				%ul
					%li 
						= p.ruolo
					%li 
						= p.user.nick

