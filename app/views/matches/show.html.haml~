

- if @m.tipo == 1
	- @players.each do |p|
		%ul
			%ul
				%li 
					= p.ruolo
				%li 
					= p.user.nick

%form{:action => "https://localhost:3000/users/1/matches/"+@m.id.to_s+"/endgame", :method => "post"}
	%p
		%input{:type => "text", :name => "punt1"}/
		%input{:type => "text", :name => "punt2"}/
		%input{:type => "submit", :value => "endgame"}/

%form{:action => "https://localhost:3000/users/1/matches/"+@m.id.to_s+"/leave", :method => "get"}
	%p
		%input{:type => "submit", :value => "leave"}/
%form{:action => "https://localhost:3000/redirect", :method => "get"}
	%p
		%input{:type => "submit", :value => "autentica google maps"}/
%form{:action => "https://localhost:3000/users/1/matches/"+@m.id.to_s+"/calendar", :method => "post"}
	%p
		%input{:type => "submit", :value => "aggiungi al calendario"}/
		
- if @m.tipo == 2
	- @single_players.each do |sp|
		%ul
			%ul
				%li 
					= p.ruolo
				%li 
					= p.user.nick
	- if @m.pt.team != nil
		SQUADRA
		= m.pt.team.nome
		- @team_players.each do |sp|
			%ul
				%ul
					%li 
						= p.ruolo
					%li 
						= p.user.nick
- if @m.tipo == 3
	- @players_team_1.each do |p|
		%ul
			%ul
				%li 
					= p.ruolo
				%li 
					= p.user.nick
	- if @m.tt.team.size == 2
		- @players_team_2.each do |p|
			%ul
				%ul
					%li 
						= p.ruolo
					%li 
						= p.user.nick

